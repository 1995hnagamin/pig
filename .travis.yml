sudo: required
dist: trusty
language: cpp
compiler:
  - clang-3.5
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
script:
  - export CXX="clang++-3.5";
  - $CXX --version
  - mkdir build
  - cd build
  - cmake ..
  - VERBOSE=1 make
